```
#include<iostream>
using namespace std;
int main ()
{
	int n,m,r,c;
	cin >> n >> m >> r >> c;
	int x1,y1,x2,y2;
	cin >> x1 >> y1 >> x2 >> y2;
	int result = (x2 / r - x1 / r + 1) * (y2 / c - y1 / c + 1);
	cout << result << endl;
	return 0;
}
```
