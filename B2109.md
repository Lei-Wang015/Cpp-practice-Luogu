```
#include<iostream>
#include<string>
#include<cctype>
using namespace std;

int main ()
{
	string s;
	getline(cin, s);
	int digit = 0;
	int len = s.size();
	for(int i = 0;i < len; i++){
		if(isdigit(s[i])){
			digit ++;
		}
	}
	cout << digit << endl;
	return 0;
}
```
